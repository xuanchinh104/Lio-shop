import { DaysAgoDirective } from './days-ago.directive';

describe('DaysAgoDirective', () => {
    it('should create an instance', () => {
        const directive = new DaysAgoDirective();
        expect(directive).toBeTruthy();
    });
});
