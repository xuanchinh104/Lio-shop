export enum ColumnTypeEnum {
    PRIMARY = 'primary',
    BOOLEAN = 'boolean',
    DATE = 'date',
    NUMBER = 'number',
    CURRENCY = 'currency',
    AVATAR = 'avatar',
    COLOR = 'color',
    ENUM = 'enum',
    TIME = 'time',
}

export enum ColumnPositionEnum {
    LEFT = 'left',
    CENTER = 'center',
    RIGHT = 'right',
}
